package Services;

import Models.User;
import Models.Transaction;
import Models.User;

public class WalletService {

//    // Thêm thu nhập vào ví
//    public void addIncome(User user, double amount) {
//        user.getWallet().setBalance(user.getWallet().getBalance() + amount);
//        System.out.println("Added " + amount + " into wallet. Current balance: " + user.getWallet().getBalance());
//    }
//
//    // Trừ chi phí từ ví
//    public boolean deductExpense(User user, double amount) {
//        if (user.getWallet().getBalance() >= amount) {
//            user.getWallet().setBalance(user.getWallet().getBalance() - amount);
//            System.out.println("Minus " + amount + " from wallet. Remaining balance: " + user.getWallet().getBalance());
//            return true;
//        } else {
//            System.out.println("Insufficient balance to complete this transaction.");
//            return false;
//        }
//    }


}
